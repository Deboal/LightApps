Drop jsmolka's `gba-tests` ROMs here (`arm.gba`, `thumb.gba`, `memory.gba`)
and `cargo test` picks them up. They are not committed: they belong to
<https://github.com/jsmolka/gba-tests> and are fetched, not vendored.

`*.gba` in this directory is gitignored.
