[workspace]
resolver = "2"
members = ["crates/gba-core", "crates/gba-headless"]

[workspace.package]
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
